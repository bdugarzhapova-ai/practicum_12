def odd_list(a, n):
    """
    Возвращает список чётных значений из списка a (первые n элементов).
    """
    if n == 0:
        return []

    result = odd_list(a, n - 1)

    if a[n - 1] % 2 == 0:
        result.append(a[n - 1])

    return result


print(odd_list([1, 2, 3, 4, 5, 6], 6))
