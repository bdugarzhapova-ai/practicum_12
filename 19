def count(a, b):
    """
    Определяет количество квадратов, отсекаемых от прямоугольника
    """

    if a == b:
        return 1

    if a < b:
        a, b = b, a

    if a % b == 0:
        return a // b
    else:
        return a // b + count(a % b, b)


print(count(4, 1))
