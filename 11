def ind_maxlist(a):
    """
    Находит индекс максимального элемента (компактная версия).
    """
    if len(a) == 1:
        return 0

    max_index_rest = ind_maxlist(a[1:]) + 1

    if a[0] > a[max_index_rest]:
        return 0
    return max_index_rest


print(ind_maxlist([3, 7, 2, 9, 5]))
