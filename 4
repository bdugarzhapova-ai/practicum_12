def sum_progress(a1, r, n):
    """
    Находит сумму первых n членов арифметической прогрессии рекурсивно
    """
    if n == 1:
        return a1
    return sum_progress(a1, r, n - 1) + (a1 + (n - 1) * r)


print(sum_progress(1, 2, 5))
