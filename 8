def fib(k):
    """
    Вычисляет k-й член последовательности Фибоначчи рекурсивно.
    """
    if k == 0:
        return 0
    if k == 1:
        return 1

    return fib(k - 1) + fib(k - 2)


print(fib(9))
