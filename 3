def progress(a1, r, n):
    """
        Находит n-й член арифметической прогрессии рекурсивно
    """
    if n == 1:
        return a1
    return progress(a1, r, n - 1) + r


print(progress(1, 2, 5))
